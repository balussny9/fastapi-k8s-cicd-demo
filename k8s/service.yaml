apiVersion: v1
kind: Service
metadata:
  name: fastapi-backend-service
  namespace: default
spec:
  selector:
    app: fastapi-backend
  ports:
    - port: 8000
      targetPort: 8000
  type: NodePort
